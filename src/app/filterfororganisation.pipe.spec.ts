import { FilterfororganisationPipe } from './filterfororganisation.pipe';

describe('FilterfororganisationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterfororganisationPipe();
    expect(pipe).toBeTruthy();
  });
});
